include ../helpers/mixins.pug
include ../../blocks/modules/moderator/messenger/messenger.pug

+item-message(true)
+item-message(true)
+item-message(data)
+item-message(true)
+item-message(data)
+item-message(true)
+item-message(data)
+item-message(true)
+item-message(data)
+item-message(true)
+item-message(data)
+item-message(true)
 